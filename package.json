{
  "name": "playwright-saucedemo",
  "scripts": {
    "test": "playwright test",
    "sanity": "playwright test tests/sanity.spec.ts",
    "headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}